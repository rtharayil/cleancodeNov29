var cities = new[] { "Austin", "New York", "San Francisco" };
 
for (var city = 0; i < cities.Count(); country++)
{
    var selectedCity = cities[i];
    DoStuff();
    DoSomeOtherStuff();
    Dispatch(selectedCity);
}
